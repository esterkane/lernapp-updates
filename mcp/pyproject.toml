[project]
name = "lernapp-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["mcp[cli]>=1.2,<2", "httpx>=0.27", "pyyaml>=6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lernapp_mcp"]
